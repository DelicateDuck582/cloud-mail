# 🔒 最终安全审计报告 — CloudMail ↔ Stalwart ↔ 雷鸟 镜像系统

**审计日期**：2026-08-29
**审计范围**：`stalwart-sync/`、`thunderbird/`、`mail-worker/`（后端）、`mail-vue/`（前端）、VPS 运行环境
**审计类型**：代码审计 / 安全审计 / 性能审计 / 密钥审计 / 语法检查

---

## 1. 语法检查 ✅

| 文件 | 结果 |
|---|---|
| `stalwart-sync/sync.js` | ✅ node --check 通过 |
| `stalwart-sync/smtp-void.js` | ✅ node --check 通过 |
| `thunderbird/background.js` | ✅ node --check 通过 |
| `thunderbird/options.js` | ✅ node --check 通过 |

---

## 2. 源代码密钥扫描 ✅（无硬编码真实密钥泄露）

| 位置 | 结果 |
|---|---|
| git 历史（全量 `-S 'eyJhIjoi'` 搜索）| ✅ 无真实 Tunnel/JWT token |
| 已 push 版本 `44a1254` 部署 MD | ✅ 均为占位符 |
| 当前工作区部署 MD | ✅ 无真实 token |
| `mail-worker/wrangler-test.toml`（fork）| ✅ `CHANGE_ME` 占位符 |
| `mail-worker/wrangler-action.toml` | ✅ `${VAR}` 环境变量引用 |
| `mail-worker/wrangler-dev.toml`（fork）| ✅ 已 gitignore，未被跟踪 |
| `mail-vue/.env.dev / .release / .remote` | ✅ 仅 VITE_* 前端配置，无密钥 |

### ⚠️ 发现 1：旧副本 `web开发/CloudMail/mail-worker/wrangler-dev.toml`
- 含**真实** `jwt_secret` 与 `ATT_SIGN_SECRET`
- **该目录不是 git 仓库**（未进版本控制）→ 未泄露到远端
- **处置**：妥善保管该目录；勿 push/共享；如需归档请先替换为占位符

### ⚠️ 发现 2：部署 MD 曾被 git 跟踪
- `stalwart部署-阿里云CLI助手.md` 含部署凭据说明 → **已执行 `git rm --cached` + 加入 `.gitignore`**
- git 历史确认无真实 token（远端安全）
- **处置**：下次 commit 提交此次删除即可

---

## 3. CloudMail 数据安全（后端 mail-worker）✅

| 攻击面 | 防护（已核实代码） |
|---|---|
| **越权读邮件** | `/email/detail`：`emailRow.userId !== userId → 403` |
| **越权删除/恢复** | `/email/delete`、`/email/restore` 传 `userId`；SQL 层 `eq(att.userId, userId)` |
| **越权标已读** | `/email/read` 同上 |
| **发信越权** | `/email/send`、`/email/import-sent` 按登录用户隔离 |
| **OAuth 越权** | bindUser/bindToken 漏洞已修复（历史记录） |
| **SQL 注入** | drizzle 参数化 / `prepare().bind()` 全量 |
| **XSS** | 入库白名单清洗 + 前端渲染兜底 |
| **登录爆破** | 5 次/10 分钟 + 失败延迟 + 成功清零 |

---

## 4. COS 安全 ✅（无权访问 / 越权访问 / 盗刷流量）

| 威胁 | 防护 |
|---|---|
| **无权访问** | COS 桶**私有读写**；`/oss/*`、`/attachments/*` 强制 HMAC 签名 |
| **越权下载** | 签名绑定 key + 15min TTL；签名校验在缓存命中前 + 常量时间比较 |
| **盗刷流量** | 签名 URL 15min 过期；Worker per-IP 限流 120 次/分；`Cache-Control: private`（防边缘缓存绕过验签）|
| **sync.js 附件拉取** | 仅 fetch 自己邮件的签名 URL，不持有 COS 密钥 |

---

## 5. sync.js 安全/性能审计 ✅

### 安全
- 凭据：env（600 root）+ 内存，不落代码
- JMAP/SMTP：强制回环（`http:` 非回环拒绝启动）
- 触发服务：`127.0.0.1:9999` + token 校验（403）
- 注入防护：`cleanRcpt`（SMTP 注入）、`cleanHeaderField`（header 注入）
- MIME boundary：已改为 `crypto.randomBytes`（**防邮件头走私**，本次修复）
- 状态文件：原子写（tmp+rename），无敏感内容

### 性能
- 按需触发 + 1h 兜底（Worker 调用大减）
- 幂等（synced/keyToSid）、失败熔断、退避重试
- 附件上限：单文件 10MB / 单封 25MB（防内存/COS 流量）

---

## 6. 雷鸟插件 ✅

- token 仅存 `browser.storage.local`（background 持有）
- 触发仅 `sync.duckgame-play.top`（host permission 最小化）
- 设置页可配置 URL/token

---

## 7. VPS 审计清单（待执行）

```bash
# ① 开放端口 vs 实际监听（公网应仅 993/465/443/8443）
ss -tlnp | grep LISTEN

# ② 防火墙（ufw）
ufw status verbose

# ③ 非 root 运行 + 用户检查
ps aux | grep -E 'stalwart|node' | grep -v grep
getent passwd | grep -E 'cloudsync|stalwart'

# ④ cron / 定时任务（防植入）
crontab -l; ls -l /etc/cron.d/

# ⑤ 日志中的异常（爆破/异常 IP）
journalctl -u stalwart -n 500 | grep -iE 'authentication failed|banned' | tail
journalctl -u cloudmail-sync -n 300 | grep -iE '异常|失败' | tail

# ⑥ 敏感文件权限
ls -l /etc/cloudmail-sync.env /etc/stalwart/config.json
ls -l /etc/letsencrypt/archive/imap.duckgame-play.top/privkey*.pem
ls -l /var/lib/cloudmail-sync/state.json

# ⑦ Stalwart 配置密钥确认
cat /etc/stalwart/config.json
```

---

## 8. 结论

- ✅ **无真实密钥泄露**（git 历史/工作区均安全）
- ✅ **无越权/盗刷/无权访问漏洞**（后端归属校验 + COS 签名 + 限流全链路）
- ✅ **语法全部通过**
- ✅ **性能可控**（按需同步）
- 🟡 建议：执行 §7 的 VPS 审计清单；`CloudMail/` 旧副本密钥妥善保管

---

# 二期审计（2026-08-29 补充 · 镜像增强后）

## 9. 新增功能安全审查 ✅

| 功能 | 安全分析 |
|---|---|
| **内嵌图 cid 嵌入** | 只下载含 `/attachments/` 的 src（COS 附件域），外部 URL 保留不下载 → **无 SSRF**；下载失败跳过不影响邮件 |
| **魔数嗅探** `sniffImageType` | 仅读文件头字节判断类型，不执行内容 → 安全；非图片（PDF 等）不误判 |
| **样式注入** `inlineEmailStyles` | 白名单 CSS 属性（max-width/height/margin/字体/颜色），不注入脚本 → 安全 |
| **RFC 2047 头编码** | 非 ASCII 头字段 encoded-word，防头注入（已剥离 CRLF）✅ |
| **指数退避重试** | 抗 VPS→CF 网络波动，失败熔断防刷屏 ✅ |
| **SIGTERM 优雅保存** | 防中断丢 state 幂等映射 → 防重复导入 ✅ |

## 10. 性能审计 ✅

| 项 | 状态 |
|---|---|
| 按需触发 + 1h 兜底 | Worker 调用大幅下降 ✅ |
| 指数退避（600ms→12s）| 覆盖瞬时波动，避免重灌中断 ✅ |
| 幂等（synced/keyToSid）| 重灌安全，不重复 ✅ |
| 附件/内嵌图串行下载 | 重灌慢但可接受（一次性）✅ |

## 11. 最终密钥/本地信息确认 ✅

- **git grep 全量扫描**（已跟踪文件）：VPS IP / token / 密码 / 本地路径 → **零命中**
- 部署 MD（含凭据说明）：已移出 git 跟踪 + gitignore ✅
- `CloudMail/` 旧副本 `wrangler-dev.toml`（非 git，本地）：含真实 jwt_secret/ATT_SIGN_SECRET → **妥善保管勿共享** ⚠️
- 触发 token 已轮换（旧值作废）✅
- Stalwart 账户密码：已统一用于 IMAP/SMTP/JMAP（**真实值不入仓库**）✅

## 12. VPS 审计结果（已执行）

| 项 | 结果 |
|---|---|
| 端口监听 | 公网仅 993/465（邮件）+ 443/8443（V2Ray/CF）+ 22（SSH，用户控制）✅ |
| ufw | allow 22/443/993/465，default deny ✅ |
| 非 root 运行 | stalwart/sync/smtp-void 均非 root ✅ |
| cron | 仅腾讯云组件 + acme.sh，无植入 ✅ |
| 爆破日志 | 无 authentication failed/banned ✅ |
| 恢复管理员 | ⚠️ 记得移除 `STALWART_RECOVERY_ADMIN`（后门）|

## 13. 二期结论

- ✅ 新增功能（内嵌图/样式/魔数/编码）无安全/性能问题
- ✅ 无密钥泄露、无本地信息外泄
- ✅ VPS 边界一致、无爆破
- 🟡 待办：移除恢复管理员；`CloudMail/` 旧副本密钥处置；8080 回环绑定（可选）
