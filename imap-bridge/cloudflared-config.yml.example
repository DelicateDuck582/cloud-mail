# cloudflared 配置示例：把本地 IMAPS(993) 通过 Tunnel 暴露为 imap.duckgame-play.top:993
# 放到 /etc/cloudflared/config.yml
tunnel: YOUR_TUNNEL_ID
credentials-file: /etc/cloudflared/YOUR_TUNNEL_ID.json

ingress:
  - hostname: imap.duckgame-play.top
    service: tcp://127.0.0.1:993
  - service: http_status:404
